import Notifications from "./notification.js"

export const metadata = {
	title: "Notifications | Tick It",

	description: "View project updates, task activity, follower interactions, and community notifications."
};

export default function NotificationsPage() {
	return (
		<Notifications/>
	)
}
